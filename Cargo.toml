[package]
name = "krnl-rs"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[profile.dev]
panic = "abort" # No backtrace for panics

[profile.release]
panic = "abort" # No backtrace for panics
